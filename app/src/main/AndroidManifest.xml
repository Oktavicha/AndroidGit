<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salsabilla.appatm_ocha">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <activity android:name=".Activity_pilihanBank" />
            <activity android:name=".Penarikan" />
            <activity android:name=".penyetoran" />
            <activity android:name=".Transfer" />
            <activity android:name=".CekSaldo" />
        </activity>
    </application>

</manifest>